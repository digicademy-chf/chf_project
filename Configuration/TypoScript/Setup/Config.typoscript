// Global site configuration
config {

    // Define the website/page title seperator and add space around it
    pageTitleSeparator = -
    pageTitleSeparator.noTrimWrap = | | |

    // Display admin panel to logged-in backend users
    admPanel = 1
}
